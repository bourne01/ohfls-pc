<template>
    <div>
        This is Edit User
    </div>
</template>

<script>
export default {
    data(){
        return{

        }   
    },
    methods:{
        /**这里写自定义函数
         * 要求如下：
         * 1、函数名称，用英文单词或易懂缩写，如果有两个或
         * 以上单词或缩写，则要求从第二个及后续的单词或缩写的首字母大写
         * 如：getDate
         * 2、函数名称要求第一个单词为动词，如果获取数据用'get',
         * 设置用'set',编辑用'edit',删除用'delete',等等
         * 3、函数的开头之前要写明函数的功能，参数的注释，以及返回值的注释
         * 如下形式：
         * /**@function 函数功能
         * *@param {参数的注释} 参数的名称
         * *@returns {返回值注释}
         * getData(){
         *  let i=0;//。。。
         * } 
         * 4、函数中的变量写明注释，变量名称要求用英文单词或易懂缩写，如有两个
         * 及以上的单词构成，则从第二及后续单词或缩写的首字母大写，如:isNull
         * 5、变量的名称要起的有意义尽量跟变量的类型相关，如：布尔类型的，用is
         * 或has开头，对象型的用obj开头，数组型的用arr开头或List结尾等等
         * 6、函数中的代码块写明注释
         */
    },
    created(){
        /**@function  
         * @param {用户Id} Id
         * @param {用户Id} Id
         * @param {用户Id} Id
         * @returns 
        */
    },
    mounted(){

    }
}
</script>

<style scoped>
    /*这里写样式
    注意：
        1、样式的名称都用英文单词，且都是小写，都统一用类(class)选择器
        2、如果样式名称有两个或以上英文组成，如are you okay,
           则中间用'-'链接，写好的样子：'are-you-okay'
        3、http://codeguide.bootcss.com/
    */
</style>

